'''import re
str = "test fletching \[fletching\] \[hand cart\]"
str = str.replace("\\","")
print(str)
start_pos = 0
end_pos = 0

word = re.findall(r"\[([A-Za-z0-9\\\s]+)\]", str)

print(word)

#for i in range(0, len(tech_keys)):
    #if
     #   if tech_keys[i] in top_comment.body:
      #      top_comment.reply(tech_keys[i] +)
            # print(top_comment.body)
            # print(scraping.tech_all[tech_keys[i]] + "\nThis action was performed by a bot, I reply to all top level comments that mentions any of the technologies in aoe2 with their corresponding info")'''


lis = []

if len(lis)==0:
    print("empty")
else:
    print("not")